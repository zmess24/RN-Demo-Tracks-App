import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

export default AccontScreen = () => {
    return <Text style={{ fontSize: 48}}>Accont Screen</Text>;
};

const styles = StyleSheet.create({});
